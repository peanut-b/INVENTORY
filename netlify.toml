[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "api"
  node_bundler = "esbuild"
  included_paths = ["api/*"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200